type User {
    id: ID!
    email: String!
    addresses: [Address!]!
    documents: [Document!]!
}

input CreateUser {
    email: String!
    password: String!
    address: CreateAddress!
    document: CreateDocument!
}

type RootMutation {
    createUser(user: CreateUser!): User!
    createUserDocument(user: ID!, document: CreateDocument!): Document!
    updateDocument(id: ID!, document: UpdateDocument!): Document!
    createUserAddress(user: ID!, address: CreateAddress!): Address!
    updateAddress(id: ID!, address: UpdateAddress!): Address!
}
